name = "maolin-ai-review"
compatibility_date = "2024-09-23"

# 环境变量
[vars]
SYNC_KEY = "maolin-sync-2026"

# KV namespace binding (teachers data cache)
# 需要先通过 wrangler 创建: wrangler kv:namespace create "TEACHERS_KV"
# 然后将返回的 id 填入下方
# [[kv_namespaces]]
# binding = "TEACHERS_KV"
# id = "your_kv_namespace_id"
